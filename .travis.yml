anguage: elixir

elixir:
  - 1.4.1

addons:
  postgresql: '9.4'

services:
  - postgresql

before_script:
  - cp config/travis.exs config/test.exs
  - mix ecto.create
  - mix ecto.migrate

script:
  - mix test